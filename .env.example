# Server-side API address (container-internal in production)
API_INTERNAL_URL=http://localhost:4201
# Public URLs (canonical links; browser-side search calls)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4201
# Must match the API's REVALIDATE_SECRET
REVALIDATE_SECRET=dev-revalidate-secret
